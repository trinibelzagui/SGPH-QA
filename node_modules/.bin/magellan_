#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../testarmada-magellan/bin/magellan" "$@"
  ret=$?
else 
  node  "$basedir/../testarmada-magellan/bin/magellan" "$@"
  ret=$?
fi
exit $ret
